# Software Scope

The public software scope consists of `examples/inspect_dataport_export.py`, a validated standard-library utility for checking a locally extracted IEEE DataPort package, and its unit tests.

The utility is read-only. It does not download data, train a model, control hardware, or reproduce the complete experimental pipeline.
