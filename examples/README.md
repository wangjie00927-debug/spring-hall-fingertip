# Examples

## Inspect a downloaded DataPort package

```powershell
python inspect_dataport_export.py --package PATH_TO_EXTRACTED_DATAPORT_PACKAGE
```

Optional JSON output:

```powershell
python inspect_dataport_export.py --package PATH_TO_EXTRACTED_DATAPORT_PACKAGE --json
```

The script is read-only and uses only the Python standard library.
